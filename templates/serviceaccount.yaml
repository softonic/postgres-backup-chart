{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "postgres-backup.serviceAccountName" . }}
  labels:
{{ include "postgres-backup.labels" . | indent 4 }}
{{- end }}
